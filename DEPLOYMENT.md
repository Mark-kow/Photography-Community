# 快速部署指南

## 🚀 一键部署步骤

### 第一步：环境检查

确保你的系统已安装以下软件：

```bash
# 检查 Node.js 版本（需要 >= 16.0.0）
node --version

# 检查 MySQL 版本（需要 >= 5.7）
mysql --version

# 检查 Redis（需要 >= 5.0）
redis-cli --version
```

如果没有安装，请先安装对应软件。

### 第二步：启动 Redis

```bash
# 启动 Redis 服务器
redis-server

# 或者在后台运行
redis-server --daemonize yes

# 检查 Redis 是否运行
redis-cli ping
# 应该返回: PONG
```

### 第三步：初始化数据库

```bash
# 方法1: 使用启动脚本（推荐）
./start.sh

# 方法2: 手动执行
mysql -u root -p < backend/database/init.sql
```

执行成功后，会创建：
- 数据库：photograph_app
- 6个数据表（users, works, comments, follows, likes, collections）
- 3个测试用户
- 4篇示例作品

### 第四步：启动后端服务

```bash
# 进入后端目录
cd backend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

看到以下提示说明启动成功：
```
╔════════════════════════════════════════╗
║   📸 摄影社区 API 服务已启动           ║
║   🚀 运行环境: development             ║
║   🌐 访问地址: http://localhost:3000  ║
║   📚 API文档: http://localhost:3000/api/v1  ║
╚════════════════════════════════════════╝
✅ MySQL数据库连接成功
✅ Redis连接成功
```

### 第五步：启动前端服务

打开新的终端窗口：

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

看到以下提示说明启动成功：
```
  VITE v5.0.8  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

### 第六步：访问应用

打开浏览器访问：
- **前端应用**: http://localhost:5173
- **后端API**: http://localhost:3000

## 🧪 测试账号

使用以下测试账号登录：

```
手机号: 13800138001
密码: 123456
```

其他测试账号：
- 13800138002 / 123456
- 13800138003 / 123456

## ✅ 验证功能

登录后可以测试以下功能：

1. **浏览作品** - 首页查看作品列表
2. **作品详情** - 点击作品查看详细信息
3. **发布作品** - 点击"发布作品"按钮上传图片
4. **点赞收藏** - 对作品进行点赞和收藏
5. **评论互动** - 在作品下发表评论
6. **关注用户** - 访问其他用户主页并关注
7. **个人中心** - 查看个人资料和作品

## 🔧 常见问题

### 1. 端口被占用

如果 3000 或 5173 端口被占用：

**后端**:
```bash
# 修改 backend/.env 文件
PORT=3001  # 改为其他端口
```

**前端**:
```bash
# 修改 frontend/vite.config.js
server: {
  port: 5174  # 改为其他端口
}
```

### 2. 数据库连接失败

检查 `backend/.env` 文件中的数据库配置：
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password  # 修改为你的MySQL密码
DB_NAME=photograph_app
```

### 3. Redis 连接失败

确保 Redis 服务已启动：
```bash
redis-cli ping
# 应该返回: PONG
```

如果返回错误，启动 Redis：
```bash
redis-server
```

### 4. 上传图片失败

确保 uploads 目录存在并有写权限：
```bash
mkdir -p backend/uploads/works
chmod 755 backend/uploads
```

### 5. 前端请求失败

检查前端是否正确代理到后端：
- 前端: http://localhost:5173
- 后端: http://localhost:3000
- 代理配置在 `frontend/vite.config.js`

## 📝 开发模式说明

### 后端热重载
使用 nodemon，修改代码后自动重启服务器。

### 前端热重载
使用 Vite HMR，修改代码后浏览器自动刷新。

### 验证码显示
开发模式下，验证码会在以下位置显示：
- 后端控制台
- 前端消息提示（持续10秒）

## 🎯 下一步

成功部署后，你可以：

1. **体验功能** - 使用测试账号体验所有功能
2. **查看代码** - 学习项目代码结构和实现
3. **二次开发** - 基于现有代码开发新功能
4. **部署上线** - 参考生产环境部署文档

## 📚 更多文档

- [README.md](README.md) - 项目介绍和完整文档
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - 项目开发总结
- [backend/database/init.sql](backend/database/init.sql) - 数据库结构

## 🆘 需要帮助？

如果遇到问题：
1. 检查控制台错误信息
2. 查看日志文件
3. 参考文档说明
4. 提交 Issue

---

**祝你使用愉快！📸**
