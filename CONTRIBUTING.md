# 贡献指南

感谢您对摄影社区项目的关注！我们欢迎所有形式的贡献，包括但不限于：

- 🐛 报告 Bug
- 💡 提出新功能建议
- 📝 改进文档
- 🔧 提交代码修复
- ✨ 实现新功能

---

## 📋 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [提交规范](#提交规范)
- [Pull Request 流程](#pull-request-流程)

---

## 🤝 行为准则

### 我们的承诺

为了营造一个开放和友好的环境，我们作为贡献者和维护者承诺：

- 使用友好和包容的语言
- 尊重不同的观点和经验
- 优雅地接受建设性批评
- 关注对社区最有利的事情
- 对其他社区成员表示同情

### 不可接受的行为

- 使用性别化的语言或图像
- 人身攻击或侮辱性评论
- 公开或私下骚扰
- 未经许可发布他人的私人信息
- 其他不道德或不专业的行为

---

## 🚀 如何贡献

### 报告 Bug

在提交 Bug 报告之前，请先搜索现有的 [Issues](https://github.com/Mark-kow/photography-community/issues) 确认问题是否已被报告。

如果是新问题，请创建 Issue 并包含以下信息：

- **清晰的标题**：简洁描述问题
- **详细描述**：包含重现步骤
- **预期行为**：说明应该发生什么
- **实际行为**：说明实际发生了什么
- **环境信息**：
  - 操作系统
  - Node.js 版本
  - 浏览器版本
- **截图**：如果适用
- **错误日志**：相关的错误信息

### 功能建议

我们欢迎新功能建议！在提交之前：

1. 确认该功能尚未在 [Issues](https://github.com/Mark-kow/photography-community/issues) 中提出
2. 创建新 Issue，使用 "Feature Request" 标签
3. 详细描述：
   - 功能的目的和价值
   - 预期的用户体验
   - 可能的实现方案
   - 相关的界面设计（如果有）

---

## 💻 开发流程

### 1. Fork 项目

点击项目页面右上角的 "Fork" 按钮，将项目 Fork 到你的账户下。

### 2. 克隆仓库

```bash
git clone https://github.com/Mark-kow/photography-community.git
cd photography-community
```

### 3. 添加上游仓库

```bash
git remote add upstream https://github.com/original-owner/photography-community.git
```

### 4. 创建分支

```bash
# 从 main 分支创建新分支
git checkout -b feature/your-feature-name

# 分支命名规范：
# feature/xxx  - 新功能
# fix/xxx      - Bug 修复
# docs/xxx     - 文档更新
# refactor/xxx - 代码重构
# test/xxx     - 测试相关
```

### 5. 安装依赖

```bash
# 后端
cd backend
npm install

# 前端
cd ../frontend
npm install
```

### 6. 配置环境

```bash
# 复制并配置环境变量
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env

# 编辑 .env 文件，配置数据库等信息
```

### 7. 初始化数据库

```bash
cd backend/database
chmod +x migrate.sh
./migrate.sh
```

### 8. 启动开发服务器

```bash
# 启动后端（终端1）
cd backend
npm run dev

# 启动前端（终端2）
cd frontend
npm run dev
```

### 9. 开发和测试

- 编写代码
- 添加测试
- 确保所有测试通过
- 手动测试功能

### 10. 提交代码

```bash
git add .
git commit -m "feat: add some feature"
git push origin feature/your-feature-name
```

### 11. 创建 Pull Request

访问你的 Fork 仓库页面，点击 "New Pull Request" 按钮。

---

## 📝 代码规范

### JavaScript/Node.js 规范

- 使用 **ES6+** 语法
- 使用 **2 个空格** 缩进
- 使用 **单引号** 表示字符串
- 使用 **分号** 结尾
- 变量命名使用 **驼峰命名法**
- 常量使用 **大写下划线** 命名

示例：

```javascript
// 良好示例
const getUserById = async (userId) => {
  const user = await User.findById(userId);
  return user;
};

// 避免
const get_user_by_id = async (user_id) => {
  const User = await User.findById(user_id)
  return User
}
```

### React 组件规范

- 组件名使用 **PascalCase**
- 文件名与组件名一致
- 使用 **函数式组件** 和 **Hooks**
- Props 使用 **驼峰命名法**
- 组件拆分遵循 **单一职责原则**

示例：

```jsx
// 良好示例
import React, { useState } from 'react';

const UserProfile = ({ userId, userName }) => {
  const [isLoading, setIsLoading] = useState(false);
  
  return (
    <div className="user-profile">
      <h2>{userName}</h2>
    </div>
  );
};

export default UserProfile;
```

### CSS 规范

- 使用 **CSS Modules** 或 **独立 CSS 文件**
- 类名使用 **kebab-case**
- 避免使用 **!important**
- 合理使用 **CSS 变量**

示例：

```css
/* 良好示例 */
.user-profile {
  padding: 20px;
  background-color: var(--bg-color);
}

.user-profile__avatar {
  width: 100px;
  height: 100px;
  border-radius: 50%;
}
```

### 注释规范

- 复杂逻辑必须添加注释
- 函数需要添加 JSDoc 注释
- 使用中文或英文注释（保持一致）

示例：

```javascript
/**
 * 获取用户信息
 * @param {number} userId - 用户ID
 * @returns {Promise<Object>} 用户对象
 */
const getUserInfo = async (userId) => {
  // 从缓存中获取
  const cached = await redis.get(`user:${userId}`);
  if (cached) return JSON.parse(cached);
  
  // 从数据库查询
  const user = await db.query('SELECT * FROM users WHERE id = ?', [userId]);
  
  // 写入缓存
  await redis.set(`user:${userId}`, JSON.stringify(user), 'EX', 3600);
  
  return user;
};
```

---

## 📤 提交规范

### Commit Message 格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

- **feat**: 新功能
- **fix**: Bug 修复
- **docs**: 文档更新
- **style**: 代码格式调整（不影响功能）
- **refactor**: 重构（不是新功能也不是修复 Bug）
- **perf**: 性能优化
- **test**: 测试相关
- **chore**: 构建过程或辅助工具的变动

### Scope 范围（可选）

- **backend**: 后端相关
- **frontend**: 前端相关
- **api**: API 接口
- **ui**: 用户界面
- **db**: 数据库

### 示例

```bash
# 新功能
git commit -m "feat(frontend): add user profile page"

# Bug 修复
git commit -m "fix(backend): resolve authentication token expiration issue"

# 文档更新
git commit -m "docs: update installation guide in README"

# 重构
git commit -m "refactor(api): optimize database query performance"
```

---

## 🔄 Pull Request 流程

### 1. 提交前检查

- ✅ 代码符合项目规范
- ✅ 所有测试通过
- ✅ 文档已更新（如果需要）
- ✅ 没有多余的调试代码
- ✅ Commit message 符合规范

### 2. 创建 Pull Request

在 PR 描述中包含：

- **目的**：说明这个 PR 解决了什么问题
- **改动**：列出主要的代码改动
- **测试**：说明如何测试这些改动
- **截图**：如果涉及 UI 改动，提供截图
- **关联 Issue**：使用 `Closes #123` 关联相关 Issue

### 3. PR 模板示例

```markdown
## 描述
简要描述这个 PR 的目的

## 改动类型
- [ ] Bug 修复
- [ ] 新功能
- [ ] 代码重构
- [ ] 文档更新
- [ ] 其他

## 改动内容
- 改动1
- 改动2
- 改动3

## 测试
说明如何测试这些改动

## 截图（如果适用）
![screenshot](url)

## 关联 Issue
Closes #123
```

### 4. 代码审查

- 耐心等待维护者审查
- 积极响应审查意见
- 及时修改代码

### 5. 合并

通过审查后，维护者会合并你的 PR。恭喜你成为贡献者！🎉

---

## 🧪 测试指南

### 运行测试

```bash
# 后端测试
cd backend
npm test

# 前端测试
cd frontend
npm test

# 测试覆盖率
npm run test:coverage
```

### 编写测试

- 为新功能编写单元测试
- 为 Bug 修复编写回归测试
- 保持测试覆盖率 > 80%

---

## 📚 文档贡献

文档同样重要！你可以：

- 修正拼写和语法错误
- 改进文档结构
- 添加示例代码
- 翻译文档

---

## ❓ 需要帮助？

如果在贡献过程中遇到问题：

- 查看 [开发指南](DEVELOPMENT_GUIDE.md)
- 查看 [故障排查](TROUBLESHOOTING.md)
- 在 [Issues](https://github.com/Mark-kow/photography-community/issues) 中提问
- 发送邮件至: mr.kow@icloud.com

---

## 🙏 感谢

感谢每一位贡献者的付出！你们让这个项目变得更好。

<div align="center">

**[⬆ 回到顶部](#贡献指南)**

</div>
